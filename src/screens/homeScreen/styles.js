import { StyleSheet } from 'react-native';

export const styles = props =>
  StyleSheet.create({
    container: {
      flex: 1
    },
    // mainText: {
    //   ...props.Theme.createAccount.mainText,
    //   marginTop: 31,
    //   marginBottom: 31,
    // },
  });
